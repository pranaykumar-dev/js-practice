let stringNumber = 33
let str = String(stringNumber) //used to covert number to string
// console.log(str);
// console.log(typeof str);

let numberOf = "33a"
let numberIs = Number(numberOf)
// console.log(typeof numberIs);
// console.log(numberIs); //NaN means Not a Number when there is a combination of string and numbers together 

let nIs = null  //null gives null only 
// console.log(nIs);

let booleanValue = undefined // when its true we get true when its false we get false but when its 1 then we get true and when its 0 we get false  
// when its emty we get false when its a sring then also we get true 
// also when its null and undefined we get false 
let booleanName = Boolean(booleanValue)
console.log(booleanName);
